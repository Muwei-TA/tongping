# v0.1 验收记录

验证日期：2026-09-21。数据全部为每次测试新建的虚构临时库。没有使用真实学生账号或平台密钥。

| 检查 | 结果 | 证据与边界 |
| --- | --- | --- |
| `python -m pytest -q` | **58 passed，11.50 秒** | API、真实 SQLite、并发报名、权限、宿主响应替身、备份、源码构建、MCP |
| `python scripts/build_mini.py` | 微信和 QQ 两套源码包生成成功 | 每套 8 页，未运行宿主编译器 |
| `python scripts/check.py` | **66 个 Python/JS 文件解析通过** | 含生成包；检查原生事件处理器及分层依赖 |
| `python scripts/e2e.py --bridge` | **15 项检查通过** | 同源 UI 离线渲染，真实临时 HTTP API；不是浏览器原生网络验证 |
| MCP 子进程 → 真实 HTTP | 成员可以读帖，外社成员得到工具错误 | `tests/test_mcp_live.py`，不是仅依赖替身的权限断言 |
| 桌面/移动排版 | 320、390、768、1440 px 动态页无横向溢出 | 浏览器实际布局检查；并未声称所有页通过全面无障碍审计 |
| 数据库重启与备份 | 已提交数据保留，备份通过完整性检查 | 自动化测试，不等于真实生产灾备演练 |
| 远端 CI | **GitHub Actions 完整通过** | 提交 `c63ce46`：安装、58 项测试、构建、静态检查、真实浏览器 HTTP 工作流及产物上传均成功 |

远端验收记录：[Actions run 35576212147](https://github.com/Muwei-TA/tongping/actions/runs/35576212147)，对应提交 `c63ce46da2e592be345f70fa588193028dfe1fde`，2026-09-21 08:08 UTC 完成。该次 CI 执行的是默认 `python scripts/e2e.py`，不是 `--bridge`；本地桥接结果与远端原生 HTTP 结果分开记录。后续仅补充本文等交付文档，不改变已验证实现。

## 浏览器实际操作链

成员登录 → 切换作品/全部筛选 → 查看私域动态 → 单图投稿 → 待审状态 → 社长审核 → 另一成员查看并反馈 → 社长审核反馈 → 新同学同意社规并申请 → 社长审批 → 报名并取消活动 → 社长指定继任者 → 继任者确认 → 原社长无法继续访问管理页。

同一测试检查 `<script>` 字样以文本显示、私域图片以鉴权请求加载、四个屏幕宽度布局、零 JavaScript 运行时错误。API 测试另覆盖重复投稿意图冲突、候补与并发不超员、图片归属、会话过期和跨社团访问。

`artifacts/e2e.json`、截图和本地服务日志由脚本生成，默认不进 Git；随交付压缩包提供，CI 也会保留自己的运行产物。只有虚构数据日志，日志不包含会话令牌。

## 实际遇到并解决的问题

- 初始 API 骨架的私域测试预期 401、实际 404，补齐身份与成员规则后通过。
- 浏览器环境禁止 URL 导航，因此原生 HTTP 浏览器测试被阻止。没有修改浏览器策略；改为显式 `--bridge` 测试模式。受限环境不能据此声称网络链路已验收。
- 离线浏览器上下文没有 `crypto.randomUUID`。发布意图改为使用 `crypto.getRandomValues` 创建，并保留同一请求的重试键。
- “全部”筛选曾发送空 kind，触发 422；先用浏览器回归复现，随后删除空参数，完整 15 项交互检查重新通过。
- 首次远端浏览器测试因 `wait_for_function` 的字符串求值触发严格 CSP 而失败；改用 Playwright 元素断言，不放宽应用 CSP、不跳过检查。随后完整远端 CI 通过。
- 增加独立响应模型，公开社团不再返回 owner_id，帖子不返回内部 client_id。

## 未完成的验收

微信/QQ 真正 code 交换、隐私授权、开发者工具编译、基础库兼容、真机图片/网络/生命周期、平台审核；实际部署 HTTPS/限流/大请求拦截；联网依赖漏洞扫描；全页无障碍审计；生产数据删除及灾备演练。这些不记为通过。

## 补充验证：扩展响应式布局检查（2026-09-21 17:19，本地）

`scripts/e2e.py` 新增 `layout()`：逐路由打开页面，断言 `document.documentElement.scrollWidth <= innerWidth + 1`。

| 检查 | 结果 | 证据与边界 |
| --- | --- | --- |
| `python -m pytest -q` | **58 passed** | 本轮未修改 `server/`、`web/`、`mini/` 代码 |
| `python scripts/build_mini.py` | 微信与 QQ 两套源码包生成成功 | 仍未运行宿主编译器 |
| `python scripts/check.py` | **66 个 Python/JavaScript 文件解析通过** | 含生成包 |
| `python scripts/e2e.py --bridge` | **33 项检查通过**（原 15 项 + 18 项布局检查） | 同源 UI 离线渲染 + 真实临时 HTTP API；新增社团目录、发布、活动列表、活动详情、我的投稿、作品详情、账号与交接、社长工作台、发起活动共 9 个路由 × 320 / 390 px；动态页仍为 320 / 390 / 768 / 1440 px。`artifacts/mobile-manage.png` 为 390 px 社长工作台截图 |
| 远端 CI | **未重新执行** | 本轮只改测试脚本与文档；远端结论仍以 `c63ce46` 为准 |

本机 Windows 简体中文环境复现时需设 `PYTHONUTF8=1`（`$env:PYTHONUTF8='1'`）：默认 GBK 解码会让 `test_build`、`test_mcp`、`test_mcp_live` 的 5 项子进程输出检查误报失败，设置后 58 项全绿；这是本机环境差异，不是被测行为变化。

本轮未改变"未完成的验收"清单：浏览器原生 HTTP 导航、微信/QQ 真机与平台审核仍未验证，不因本轮结果改变。
